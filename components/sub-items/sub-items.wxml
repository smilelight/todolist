<!--components/sub-items/sub-items.wxml-->
<view class='sub-container'>
  <view class='sub-item {{item.completed ? "sub-completed-item" : ""}}' wx:for="{{customData}}" wx:key="{{index}}">
    {{item.title}}
  </view>
</view>